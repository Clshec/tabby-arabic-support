import { ConfigProvider } from 'tabby-core';
export declare class ArabicConfigProvider extends ConfigProvider {
    defaults: {
        arabicSupport: {
            enabled: boolean;
            mode: string;
        };
    };
}
